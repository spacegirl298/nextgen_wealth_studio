/*Manages nudge state and delivery globally.
–	Stores: activeNudges array
–	Exposes: dismissNudge(id), addNudge(nudge)
–	Listens to FinancialContext changes and triggers rule-based nudges via useNudges hook
–	Ensures NudgeBanner has access to current nudges from anywhere in the tree
*/

export default function NudgeContext() {
  return <div>Patient Dashboard</div>
}