/*Luxury car vs Invest the difference studio container.
–	Manages all input state
–	Computes: total cost of car finance (instalments + interest + insurance), depreciated value over time
–	Computes: what the deposit + monthly difference would grow to if invested at market rate
–	Passes data to DepreciationChart and StudioVerdict

*/
import { useState, useRef, useEffect } from "react";
import { createPortal } from "react-dom";
import styles from "../../Studios.module.css";

export default function LuxuryStudio() {
  return <div>Patient Dashboard</div>
}