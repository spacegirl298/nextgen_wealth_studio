/*Helper functions specific to snapshot logic.
–	createSnapshot(financialData) — formats current data into a snapshot object with timestamp
–	compareSnapshots(snapshotA, snapshotB) — returns delta object
–	getSnapshotTrend(history) — returns trend direction per metric
–	formatSnapshotSummary(snapshot) — short display string
*/